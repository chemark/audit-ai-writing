interface:
  display_name: "Audit AI Writing"
  short_description: "审查草稿中常见的 AI 写作痕迹"
  default_prompt: "Use $audit-ai-writing to review this draft for signs of AI writing and suggest concrete edits."
policy:
  allow_implicit_invocation: true
